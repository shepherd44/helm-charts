{{- $v := include "cp-ksql-server.values" . | fromYaml -}}
Confluent ksqlDB Server is starting.

  REST API   http://{{ template "cp-ksql-server.fullname" . }}:{{ $v.service.port }}
  Mode       {{ if $v.ksql.headless }}headless{{ else }}interactive{{ end }}
  Service id {{ include "cp-ksql-server.serviceId" . }}

https://docs.confluent.io/current/ksql/docs
{{ if include "cp-ksql-server.usesLegacyValues" . }}
WARNING: this release still sets values in the pre-1.2.0 layout. They are mapped onto
the current keys and nothing is broken, but the old names will not be carried forever:

  imageTag / imagePullPolicy / imagePullSecrets           ->  image.*
  servicePort                                            ->  service.port
  prometheus.jmx.*                                       ->  metrics.*
  cp-schema-registry.url                                 ->  schemaRegistry.url
{{ end -}}
