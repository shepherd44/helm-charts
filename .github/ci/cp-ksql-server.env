# What CI has to know about this chart, sourced by .github/workflows/ci.yaml so that
# the workflow itself names no chart. $CHART is this chart's path from the repo root.
#
# This sits here rather than under charts/<name>/ on purpose: ct treats any file that
# changes inside a chart directory as a chart change and demands a version bump, and a
# CI-only edit is not worth publishing a release for.
#
# Every variable is optional; a chart with no file here is rendered with plain defaults
# and is not installed on a cluster.

# Extra `helm template` flags for the render, kubeconform and image passes. Both images
# and both ConfigMaps already render on defaults — `prometheus.jmx.enabled` and
# `ksql.headless` are on — so the only template kubeconform would otherwise never see is
# the external LoadBalancer Service.
RENDER_ARGS=(--set external.enabled=true)

# No INSTALL here yet. The chart declares no readiness probe, so `ct install --wait`
# would report success as soon as a container process existed, whether or not ksqlDB
# ever reached its command topic. Installing it would look like a check and not be one.
# This turns on in the same change that adds the probes.
